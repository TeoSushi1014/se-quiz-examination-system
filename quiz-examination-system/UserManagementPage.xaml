<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="quiz_examination_system.UserManagementPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:quiz_examination_system"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Page.Resources>
        <Color x:Key="AdminBadgeColor">#8B5CF6</Color>
        <Color x:Key="TeacherBadgeColor">#0078D4</Color>
        <Color x:Key="StudentBadgeColor">#00B7C3</Color>
        <Color x:Key="ActiveStatusColor">#107C10</Color>
        <Color x:Key="InactiveStatusColor">#605E5C</Color>
    </Page.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Padding="24,16" Spacing="12">
            <TextBlock Text="User management" Style="{StaticResource TitleTextBlockStyle}"/>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                
                <AutoSuggestBox x:Name="SearchBox" 
                                Grid.Column="0"
                                PlaceholderText="Search by username..." 
                                QueryIcon="Find"
                                TextChanged="OnSearchTextChanged"
                                MaxWidth="400"
                                HorizontalAlignment="Left"/>
                
                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8">
                    <Button Click="OnCreateUserClicked" Style="{StaticResource AccentButtonStyle}">
                        <StackPanel Orientation="Horizontal" Spacing="8">
                            <FontIcon Glyph="&#xE109;" FontSize="16"/>
                            <TextBlock Text="Create user"/>
                        </StackPanel>
                    </Button>
                </StackPanel>
            </Grid>
        </StackPanel>

        <ScrollViewer Grid.Row="1" Padding="24,0,24,24">
            <ItemsRepeater ItemsSource="{x:Bind Users, Mode=OneWay}">
                <ItemsRepeater.ItemTemplate>
                    <DataTemplate x:DataType="local:UserItem">
                        <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="8"
                                Padding="16"
                                Margin="0,0,0,12">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="Auto"/>
                                </Grid.ColumnDefinitions>

                                <StackPanel Grid.Column="0" Spacing="8">
                                    <TextBlock Text="{x:Bind Username, Mode=OneTime}" Style="{StaticResource SubtitleTextBlockStyle}"/>
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <Border Background="{x:Bind RoleBadgeColor, Mode=OneTime}" 
                                                BorderBrush="{x:Bind RoleBadgeColor, Mode=OneTime}"
                                                BorderThickness="1"
                                                CornerRadius="6" 
                                                Padding="10,4"
                                                Translation="0,0,8">
                                            <TextBlock Text="{x:Bind Role, Mode=OneTime}" 
                                                       Foreground="White" 
                                                       FontSize="12"
                                                       FontWeight="SemiBold"/>
                                            <Border.Shadow>
                                                <ThemeShadow />
                                            </Border.Shadow>
                                        </Border>
                                        <Border Background="{x:Bind StatusColor, Mode=OneWay}" 
                                                BorderBrush="{x:Bind StatusColor, Mode=OneWay}"
                                                BorderThickness="1"
                                                CornerRadius="6" 
                                                Padding="10,4"
                                                Translation="0,0,8">
                                            <TextBlock Text="{x:Bind StatusText, Mode=OneWay}" 
                                                       Foreground="White" 
                                                       FontSize="12"
                                                       FontWeight="SemiBold"/>
                                            <Border.Shadow>
                                                <ThemeShadow />
                                            </Border.Shadow>
                                        </Border>
                                    </StackPanel>
                                </StackPanel>

                                <Button Grid.Column="1" 
                                        Background="Transparent" 
                                        BorderThickness="0" 
                                        VerticalAlignment="Center"
                                        Padding="8"
                                        Tag="{x:Bind Id, Mode=OneTime}">
                                    <FontIcon Glyph="&#xE712;" FontSize="16"/>
                                    <Button.Flyout>
                                        <MenuFlyout>
                                            <MenuFlyoutItem Text="Edit role" Click="OnEditRoleClicked" Tag="{x:Bind Id, Mode=OneTime}">
                                                <MenuFlyoutItem.Icon>
                                                    <FontIcon Glyph="&#xE77B;"/>
                                                </MenuFlyoutItem.Icon>
                                            </MenuFlyoutItem>
                                            <MenuFlyoutItem Text="Reset password" Click="OnResetPasswordClicked" Tag="{x:Bind Id, Mode=OneTime}">
                                                <MenuFlyoutItem.Icon>
                                                    <FontIcon Glyph="&#xE8AC;"/>
                                                </MenuFlyoutItem.Icon>
                                            </MenuFlyoutItem>
                                            <MenuFlyoutSeparator/>
                                            <MenuFlyoutItem Text="{x:Bind BlockButtonText, Mode=OneWay}" Click="OnToggleStatusClicked" Tag="{x:Bind Id, Mode=OneTime}" Foreground="#D13438">
                                                <MenuFlyoutItem.Icon>
                                                    <FontIcon Glyph="&#xE8F8;"/>
                                                </MenuFlyoutItem.Icon>
                                            </MenuFlyoutItem>
                                        </MenuFlyout>
                                    </Button.Flyout>
                                </Button>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsRepeater.ItemTemplate>
            </ItemsRepeater>
        </ScrollViewer>

        <ProgressRing x:Name="LoadingRing" 
                      IsActive="False" 
                      Width="60" 
                      Height="60"
                      HorizontalAlignment="Center" 
                      VerticalAlignment="Center" 
                      Grid.RowSpan="2"/>

        <InfoBar x:Name="MessageBar"
                 IsOpen="False"
                 IsClosable="True"
                 VerticalAlignment="Bottom"
                 HorizontalAlignment="Right"
                 MaxWidth="400"
                 Margin="24"
                 Grid.RowSpan="2">
            <InfoBar.Shadow>
                <ThemeShadow />
            </InfoBar.Shadow>
        </InfoBar>
    </Grid>
</Page>
