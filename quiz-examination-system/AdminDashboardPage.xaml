<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="quiz_examination_system.AdminDashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:quiz_examination_system"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <ScrollViewer Padding="32,24">
        <StackPanel Spacing="24">
            <StackPanel Spacing="8">
                <TextBlock Text="Administrator Dashboard" Style="{StaticResource TitleTextBlockStyle}" />
                <TextBlock Text="Manage system users and monitor activities" Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <StackPanel Spacing="12">
                <TextBlock Text="Management Actions" Style="{StaticResource SubtitleTextBlockStyle}" />
                <Button x:Name="ManageUsersBtn" Content="Manage Users" HorizontalAlignment="Left" Click="ManageUsers_Click" Padding="16,8" Style="{StaticResource AccentButtonStyle}" />
                <TextBlock Text="View, create, reset passwords, and manage user status" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
            </StackPanel>

            <InfoBar x:Name="ActionMessage" IsOpen="False" Severity="Informational" IsClosable="True" />
        </StackPanel>
    </ScrollViewer>
</Page>
