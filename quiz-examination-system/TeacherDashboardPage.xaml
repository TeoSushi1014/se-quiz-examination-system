<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="quiz_examination_system.TeacherDashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:quiz_examination_system"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid RowSpacing="0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <NavigationView x:Name="TeacherNav" Grid.Row="0" IsBackButtonVisible="Collapsed" IsSettingsVisible="False" PaneDisplayMode="Top" SelectionChanged="TeacherNav_SelectionChanged">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Question Bank" Tag="QuestionBank">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8E6;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Manage Quizzes" Tag="ManageQuizzes">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE8C8;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
                <NavigationViewItem Content="Review Attempts" Tag="ReviewAttempts">
                    <NavigationViewItem.Icon>
                        <FontIcon Glyph="&#xE81E;" />
                    </NavigationViewItem.Icon>
                </NavigationViewItem>
            </NavigationView.MenuItems>
        </NavigationView>

        <ScrollViewer Grid.Row="1" Padding="32,24">
            <StackPanel Spacing="24">
                <StackPanel Spacing="8">
                    <TextBlock x:Name="TitleText" Style="{StaticResource TitleTextBlockStyle}" />
                    <TextBlock x:Name="DescriptionText" Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                </StackPanel>

                <StackPanel x:Name="ContentArea" Spacing="12" />

                <InfoBar x:Name="ActionMessage" IsOpen="False" Severity="Informational" IsClosable="True" />
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
