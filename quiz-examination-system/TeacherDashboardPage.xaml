<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="quiz_examination_system.TeacherDashboardPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:quiz_examination_system"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <ScrollViewer Padding="32,24,32,24">
        <StackPanel Spacing="24">
            <StackPanel Spacing="8">
                <TextBlock Text="Teacher dashboard" FontSize="28" FontWeight="SemiBold"/>
                <TextBlock Text="Manage questions, create quizzes, review student attempts and view reports" 
                           FontSize="14" 
                           Opacity="0.7"/>
            </StackPanel>

            <Grid ColumnSpacing="16" RowSpacing="16">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <Border Grid.Column="0" Grid.Row="0" 
                        Padding="24" CornerRadius="8" 
                        BorderThickness="1" MinHeight="160"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                    <StackPanel Spacing="8">
                        <FontIcon Glyph="&#xE8E6;" FontSize="32"/>
                        <TextBlock Text="Question bank" FontSize="20" FontWeight="SemiBold"/>
                        <TextBlock Text="Create and manage quiz questions" 
                                   FontSize="12" Opacity="0.7" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Grid.Row="0" 
                        Padding="24" CornerRadius="8" 
                        BorderThickness="1" MinHeight="160"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                    <StackPanel Spacing="8">
                        <FontIcon Glyph="&#xE8C8;" FontSize="32"/>
                        <TextBlock Text="Manage quizzes" FontSize="20" FontWeight="SemiBold"/>
                        <TextBlock Text="Create, edit and publish quizzes" 
                                   FontSize="12" Opacity="0.7" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="0" Grid.Row="1" 
                        Padding="24" CornerRadius="8" 
                        BorderThickness="1" MinHeight="160"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                    <StackPanel Spacing="8">
                        <FontIcon Glyph="&#xE81E;" FontSize="32"/>
                        <TextBlock Text="Review attempts" FontSize="20" FontWeight="SemiBold"/>
                        <TextBlock Text="Review and grade student quiz attempts" 
                                   FontSize="12" Opacity="0.7" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>

                <Border Grid.Column="1" Grid.Row="1" 
                        Padding="24" CornerRadius="8" 
                        BorderThickness="1" MinHeight="160"
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                    <StackPanel Spacing="8">
                        <FontIcon Glyph="&#xE9F9;" FontSize="32"/>
                        <TextBlock Text="Reports and analytics" FontSize="20" FontWeight="SemiBold"/>
                        <TextBlock Text="View exam results and export CSV reports" 
                                   FontSize="12" Opacity="0.7" TextWrapping="Wrap"/>
                    </StackPanel>
                </Border>
            </Grid>
        </StackPanel>
    </ScrollViewer>
</Page>
