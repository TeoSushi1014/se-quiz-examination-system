<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="quiz_examination_system.ReviewAttemptsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:quiz_examination_system"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent"
    Loaded="Page_Loaded">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="16" Margin="0,0,0,24">
            <TextBlock Text="Review attempts" Style="{StaticResource TitleTextBlockStyle}"/>
            
            <StackPanel Orientation="Horizontal" Spacing="12">
                <ComboBox x:Name="QuizSelector" 
                         PlaceholderText="Select quiz..."
                         MinWidth="300"
                         SelectionChanged="QuizSelector_SelectionChanged"/>
                <Button x:Name="RefreshBtn" 
                       Content="Refresh" 
                       Click="Refresh_Click"/>
                <Button x:Name="ExportCsvBtn" 
                       Content="Export to CSV" 
                       Click="ExportCsv_Click"
                       IsEnabled="False"
                       Style="{StaticResource AccentButtonStyle}"/>
                <Button x:Name="ToggleReleaseBtn"
                       Content="Release results"
                       Click="ToggleRelease_Click"
                       IsEnabled="False"
                       Visibility="Collapsed"/>
            </StackPanel>
        </StackPanel>

        <Grid Grid.Row="1">
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                   BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                   BorderThickness="1"
                   CornerRadius="8"
                   Padding="0">
                <Grid>
                    <ListView x:Name="AttemptsListView" SelectionMode="None">
                        <ListView.Header>
                            <Grid Padding="16,12" 
                                 Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                 BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                                 BorderThickness="0,0,0,1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="180"/>
                                    <ColumnDefinition Width="120"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="Student" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Grid.Column="1" Text="Attempt" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                                <TextBlock Grid.Column="2" Text="Score" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                                <TextBlock Grid.Column="3" Text="Correct" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                                <TextBlock Grid.Column="4" Text="Incorrect" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                                <TextBlock Grid.Column="5" Text="Time spent" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}" HorizontalAlignment="Center"/>
                                <TextBlock Grid.Column="6" Text="Submitted at" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Grid.Column="7" Text="Actions" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            </Grid>
                        </ListView.Header>

                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="local:AttemptItem">
                                <Grid Padding="16,12" 
                                     Background="Transparent"
                                     BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                                     BorderThickness="0,0,0,1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="100"/>
                                        <ColumnDefinition Width="150"/>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="120"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" 
                                              Text="{x:Bind StudentUsername, Mode=OneWay}" 
                                              FontWeight="SemiBold"
                                              VerticalAlignment="Center"/>

                                    <TextBlock Grid.Column="1" 
                                              Text="{x:Bind AttemptNumber, Mode=OneWay}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Center"/>

                                    <StackPanel Grid.Column="2" 
                                               Orientation="Horizontal" 
                                               Spacing="4"
                                               HorizontalAlignment="Center"
                                               VerticalAlignment="Center">
                                        <TextBlock Text="{x:Bind Score, Mode=OneWay}" FontWeight="Bold"/>
                                        <TextBlock Text="/"/>
                                        <TextBlock Text="{x:Bind TotalPoints, Mode=OneWay}"/>
                                    </StackPanel>

                                    <TextBlock Grid.Column="3" 
                                              Text="{x:Bind CorrectCount, Mode=OneWay}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Center"
                                              Foreground="Green"/>

                                    <TextBlock Grid.Column="4" 
                                              Text="{x:Bind IncorrectCount, Mode=OneWay}"
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Center"
                                              Foreground="Red"/>

                                    <TextBlock Grid.Column="5" 
                                              VerticalAlignment="Center"
                                              HorizontalAlignment="Center">
                                        <Run Text="{x:Bind TimeSpentSeconds, Mode=OneWay}"/>
                                        <Run Text="s"/>
                                    </TextBlock>

                                    <TextBlock Grid.Column="6" 
                                              Text="{x:Bind SubmittedAt, Mode=OneWay}"
                                              VerticalAlignment="Center"/>

                                    <Button Grid.Column="7" 
                                           Content="Delete" 
                                           Tag="{x:Bind AttemptId, Mode=OneWay}"
                                           Click="DeleteAttempt_Click"
                                           Padding="12,6"
                                           VerticalAlignment="Center"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>

                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                    </ListView>

                    <ProgressRing x:Name="LoadingRing"
                                 IsActive="False"
                                 Width="48"
                                 Height="48"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Center"/>

                    <TextBlock x:Name="EmptyStateText"
                              Text="No attempts found for this quiz"
                              Visibility="Collapsed"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                              FontSize="16"/>
                </Grid>
            </Border>
            
            <InfoBar x:Name="ActionMessage" 
                     IsOpen="False" 
                     IsClosable="True"
                     VerticalAlignment="Bottom"
                     HorizontalAlignment="Right"
                     MaxWidth="400"
                     Margin="24"
                     Canvas.ZIndex="999">
                <InfoBar.Shadow>
                    <ThemeShadow />
                </InfoBar.Shadow>
            </InfoBar>
        </Grid>
    </Grid>
</Page>
