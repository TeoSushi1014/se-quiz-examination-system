<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="quiz_examination_system.SystemLogsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:quiz_examination_system"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent">

    <Grid Padding="24" RowSpacing="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" 
                   Text="System audit logs" 
                   Style="{StaticResource TitleTextBlockStyle}"
                   Margin="0,0,0,8"/>

        <Grid Grid.Row="1" ColumnSpacing="12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <AutoSuggestBox Grid.Column="0"
                           x:Name="SearchBox"
                           PlaceholderText="Search by actor, action or details..."
                           QueryIcon="Find"
                           TextChanged="OnSearchTextChanged"/>

            <ComboBox Grid.Column="1"
                     x:Name="ActionFilterComboBox"
                     PlaceholderText="Filter by action"
                     MinWidth="200"
                     SelectionChanged="OnActionFilterChanged">
                <ComboBoxItem Content="All" IsSelected="True"/>
                <ComboBoxItem Content="CREATE_USER"/>
                <ComboBoxItem Content="DELETE_USER"/>
                <ComboBoxItem Content="UPDATE_USER"/>
                <ComboBoxItem Content="DISABLE_USER"/>
                <ComboBoxItem Content="ENABLE_USER"/>
                <ComboBoxItem Content="PURGE_QUIZ"/>
                <ComboBoxItem Content="LOGIN_SUCCESS"/>
                <ComboBoxItem Content="LOGIN_FAILURE"/>
            </ComboBox>

            <Button Grid.Column="2"
                   Click="OnRefreshClicked">
                <StackPanel Orientation="Horizontal" Spacing="8">
                    <FontIcon Glyph="&#xE72C;" FontSize="16"/>
                    <TextBlock Text="Refresh" VerticalAlignment="Center"/>
                </StackPanel>
            </Button>
        </Grid>

        <Grid Grid.Row="2">
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                   BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                   BorderThickness="1"
                   CornerRadius="8"
                   Padding="0">
                <Grid>
                    <ListView x:Name="LogsListView"
                             ItemsSource="{x:Bind Logs, Mode=OneWay}"
                             SelectionMode="None">
                        <ListView.Header>
                            <Grid Padding="16,12" 
                                 Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                 BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                                 BorderThickness="0,0,0,1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="180"/>
                                    <ColumnDefinition Width="150"/>
                                    <ColumnDefinition Width="180"/>
                                    <ColumnDefinition Width="120"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="Timestamp" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Grid.Column="1" Text="Actor" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Grid.Column="2" Text="Action" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Grid.Column="3" Text="Target" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                <TextBlock Grid.Column="4" Text="Details" FontWeight="SemiBold" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                            </Grid>
                        </ListView.Header>

                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="local:LogItem">
                                <Grid Padding="16,12" 
                                     Background="Transparent"
                                     BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                                     BorderThickness="0,0,0,1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="150"/>
                                        <ColumnDefinition Width="180"/>
                                        <ColumnDefinition Width="120"/>
                                        <ColumnDefinition Width="*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" 
                                              Text="{x:Bind FormattedTimestamp}"
                                              Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                              VerticalAlignment="Center"
                                              TextWrapping="NoWrap"/>

                                    <StackPanel Grid.Column="1" 
                                               VerticalAlignment="Center"
                                               Spacing="2">
                                        <TextBlock Text="{x:Bind ActorUsername}" 
                                                  FontWeight="SemiBold"
                                                  TextWrapping="NoWrap"/>
                                        <TextBlock Text="{x:Bind ActorId}" 
                                                  Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                                  FontSize="11"
                                                  TextWrapping="NoWrap"/>
                                    </StackPanel>

                                    <Border Grid.Column="2"
                                           Background="{x:Bind ActionBadgeColor}"
                                           CornerRadius="4"
                                           Padding="8,4"
                                           HorizontalAlignment="Left"
                                           VerticalAlignment="Center">
                                        <TextBlock Text="{x:Bind Action}"
                                                  Foreground="{x:Bind ActionColor}"
                                                  FontWeight="SemiBold"
                                                  FontSize="12"/>
                                    </Border>

                                    <StackPanel Grid.Column="3" 
                                               VerticalAlignment="Center"
                                               Spacing="2">
                                        <TextBlock Text="{x:Bind TargetTable}" 
                                                  Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                                  FontSize="12"
                                                  TextWrapping="NoWrap"/>
                                        <TextBlock Text="{x:Bind TargetId}" 
                                                  Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                                                  FontSize="11"
                                                  TextWrapping="NoWrap"/>
                                    </StackPanel>

                                    <TextBlock Grid.Column="4" 
                                              Text="{x:Bind Details}"
                                              VerticalAlignment="Center"
                                              TextWrapping="Wrap"
                                              MaxLines="2"
                                              TextTrimming="CharacterEllipsis"
                                              Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>

                        <ListView.ItemContainerStyle>
                            <Style TargetType="ListViewItem">
                                <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                <Setter Property="Padding" Value="0"/>
                                <Setter Property="Margin" Value="0"/>
                            </Style>
                        </ListView.ItemContainerStyle>
                    </ListView>

                    <ProgressRing x:Name="LoadingRing"
                                 IsActive="False"
                                 Width="48"
                                 Height="48"
                                 HorizontalAlignment="Center"
                                 VerticalAlignment="Center"/>

                    <TextBlock x:Name="EmptyStateText"
                              Text="No logs found"
                              Visibility="Collapsed"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Foreground="{ThemeResource TextFillColorTertiaryBrush}"
                              FontSize="16"/>
                </Grid>
            </Border>

            <InfoBar x:Name="MessageBar"
                    IsOpen="False"
                    IsClosable="True"
                    VerticalAlignment="Bottom"
                    HorizontalAlignment="Right"
                    MaxWidth="400"
                    Margin="0,0,0,16"/>
        </Grid>
    </Grid>
</Page>
