<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="quiz_examination_system.ReportsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:quiz_examination_system"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="Transparent"
    Loaded="Page_Loaded">

    <Grid Padding="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Spacing="16" Margin="0,0,0,24">
            <TextBlock Text="Quiz performance reports" Style="{StaticResource TitleTextBlockStyle}"/>
            
            <StackPanel Orientation="Horizontal" Spacing="12" VerticalAlignment="Center">
                <TextBlock Text="Select quiz:" VerticalAlignment="Center" FontWeight="SemiBold"/>
                <ComboBox x:Name="QuizSelector" 
                         PlaceholderText="Choose a quiz..." 
                         MinWidth="300"
                         SelectionChanged="QuizSelector_SelectionChanged"/>
                <Button x:Name="ExportCsvBtn" 
                       Click="ExportCsv_Click"
                       IsEnabled="False">
                    <StackPanel Orientation="Horizontal" Spacing="8">
                        <FontIcon Glyph="&#xE8DE;" FontSize="16"/>
                        <TextBlock Text="Export to CSV" VerticalAlignment="Center"/>
                    </StackPanel>
                </Button>
            </StackPanel>
        </StackPanel>

        <Grid Grid.Row="1" x:Name="SummaryCards" Visibility="Collapsed" Margin="0,0,0,24">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0" 
                   Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                   CornerRadius="8" 
                   Padding="20" 
                   Margin="0,0,8,0">
                <StackPanel Spacing="8">
                    <TextBlock Text="Total attempts" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="14"/>
                    <TextBlock x:Name="TotalAttemptsText" Text="0" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="1" 
                   Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                   CornerRadius="8" 
                   Padding="20" 
                   Margin="4,0">
                <StackPanel Spacing="8">
                    <TextBlock Text="Average score" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="14"/>
                    <TextBlock x:Name="AverageScoreText" Text="0.0" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>

            <Border Grid.Column="2" 
                   Background="{ThemeResource CardBackgroundFillColorDefaultBrush}" 
                   CornerRadius="8" 
                   Padding="20" 
                   Margin="8,0,0,0">
                <StackPanel Spacing="8">
                    <TextBlock Text="Pass rate" Foreground="{ThemeResource TextFillColorSecondaryBrush}" FontSize="14"/>
                    <TextBlock x:Name="PassRateText" Text="0%" FontSize="32" FontWeight="Bold"/>
                </StackPanel>
            </Border>
        </Grid>

        <InfoBar x:Name="ActionMessage" 
                 Grid.Row="2"
                 IsOpen="False" 
                 IsClosable="True"
                 VerticalAlignment="Bottom"
                 HorizontalAlignment="Right"
                 MaxWidth="400"
                 Margin="24">
            <InfoBar.Shadow>
                <ThemeShadow />
            </InfoBar.Shadow>
        </InfoBar>

        <Grid Grid.Row="2">
            <Border Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                   BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                   BorderThickness="1"
                   CornerRadius="8"
                   Padding="0">
                <Grid>
                    <ProgressRing x:Name="LoadingRing" IsActive="False" Width="60" Height="60"/>
            
                    <TextBlock x:Name="EmptyStateText" 
                              Text="No attempts found for this quiz" 
                              HorizontalAlignment="Center" 
                              VerticalAlignment="Center"
                              Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                              FontSize="16"
                              Visibility="Collapsed"/>
            
                    <ListView x:Name="ReportListView" SelectionMode="None">
                        <ListView.Header>
                            <Grid Padding="16,12" 
                                 Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                                 BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                                 BorderThickness="0,0,0,1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="2*"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="1.5*"/>
                                    <ColumnDefinition Width="1.5*"/>
                                    <ColumnDefinition Width="2*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="Student" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Grid.Column="1" Text="Attempt" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Grid.Column="2" Text="Score" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Grid.Column="3" Text="Time spent" FontWeight="SemiBold" FontSize="13"/>
                                <TextBlock Grid.Column="4" Text="Submitted at" FontWeight="SemiBold" FontSize="13"/>
                            </Grid>
                        </ListView.Header>
                        <ListView.ItemTemplate>
                            <DataTemplate x:DataType="local:ReportItem">
                                <Grid Padding="16,12" 
                                     BorderBrush="{ThemeResource DividerStrokeColorDefaultBrush}"
                                     BorderThickness="0,0,0,1">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="2*"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="1.5*"/>
                                        <ColumnDefinition Width="1.5*"/>
                                        <ColumnDefinition Width="2*"/>
                                    </Grid.ColumnDefinitions>

                                    <TextBlock Grid.Column="0" Text="{x:Bind StudentName, Mode=OneWay}" VerticalAlignment="Center" FontSize="13"/>
                                    <TextBlock Grid.Column="1" Text="{x:Bind AttemptNumber, Mode=OneWay}" VerticalAlignment="Center" FontSize="13"/>
                                    <TextBlock Grid.Column="2" Text="{x:Bind FormattedScore, Mode=OneWay}" VerticalAlignment="Center" FontWeight="SemiBold" FontSize="13"/>
                                    <TextBlock Grid.Column="3" Text="{x:Bind TimeSpent, Mode=OneWay}" VerticalAlignment="Center" FontSize="13"/>
                                    <TextBlock Grid.Column="4" Text="{x:Bind SubmittedDate, Mode=OneWay}" VerticalAlignment="Center" FontSize="13"/>
                                </Grid>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>
                </Grid>
            </Border>
        </Grid>
    </Grid>
</Page>
